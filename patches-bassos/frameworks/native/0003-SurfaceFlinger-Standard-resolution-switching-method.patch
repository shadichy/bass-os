From d8bd1178852c0ad36c40fc248392e67bd566ab52 Mon Sep 17 00:00:00 2001
From: Li Huang <putin.li@rock-chips.com>
Date: Thu, 17 Aug 2023 08:23:11 +0000
Subject: [PATCH 3/4] SurfaceFlinger : Standard resolution switching method.

Fix a BUG for switching resolutin by standard method.
The destinationClip parameter will be abnormal.

Bug: Defect #303103
Change-Id: I283e848cc6963eaf7c7397f6062785d97ca895a1
Signed-off-by: Li Huang <putin.li@rock-chips.com>
Signed-off-by: libin <bin.li@rock-chips.com>
---
 services/surfaceflinger/SurfaceFlinger.cpp | 7 ++++++-
 1 file changed, 6 insertions(+), 1 deletion(-)

diff --git a/services/surfaceflinger/SurfaceFlinger.cpp b/services/surfaceflinger/SurfaceFlinger.cpp
index 6bd5d6e304..50fca59813 100644
--- a/services/surfaceflinger/SurfaceFlinger.cpp
+++ b/services/surfaceflinger/SurfaceFlinger.cpp
@@ -3942,6 +3942,12 @@ void SurfaceFlinger::processDisplayChanged(const wp<IBinder>& displayToken,
         if (currentState.flags != drawingState.flags) {
             display->setFlags(currentState.flags);
         }
+
+        if (currentState.width != drawingState.width ||
+            currentState.height != drawingState.height) {
+            display->setDisplaySize(currentState.width, currentState.height);
+        }
+
         if ((currentState.orientation != drawingState.orientation) ||
             (currentState.layerStackSpaceRect != drawingState.layerStackSpaceRect) ||
             (currentState.orientedDisplaySpaceRect != drawingState.orientedDisplaySpaceRect)) {
@@ -3955,7 +3961,6 @@ void SurfaceFlinger::processDisplayChanged(const wp<IBinder>& displayToken,
         }
         if (currentState.width != drawingState.width ||
             currentState.height != drawingState.height) {
-            display->setDisplaySize(currentState.width, currentState.height);
 
             if (display->getId() == mActiveDisplayId) {
                 onActiveDisplaySizeChanged(*display);
-- 
2.43.0

